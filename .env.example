# Copy to .env (gitignored) for local Phase 5 sync client tests.
# Generate a long random secret (min 16 chars). Never commit the real value.

LEIBOT_MARKET_SYNC_API_KEY=replace-with-long-random-secret
